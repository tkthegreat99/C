#include<stdio.h>

int main() {
	int input= 0;
	int a, b, c, d, e;
	scanf("%d", &input);
	a = input / 10000;
	b = (input % 10000)/1000;
	c = (input % 1000)/100;
	d = (input % 100)/10;
	e = input % 10;
	printf("%dTTHO %dTHO %dHUN %dTEN %d", a, b, c, d, e);

	return 0;
}
